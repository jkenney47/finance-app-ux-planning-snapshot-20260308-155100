# Acceptance Criteria Template

- User can `<action>` without `<failure mode>`.
- System response stays within `<performance target>`.
- Existing `<critical behavior>` remains unchanged.
- Validation command/check: `<command or test>`.
