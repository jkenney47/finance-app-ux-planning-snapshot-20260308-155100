# Gemini Plan Review

- Plan: `docs/plans/2026-03-03-figma-xcode-ui-workflow.md`
- Generated-At-UTC: 2026-03-04T01:34:18Z
- Model: gemini-3.1-pro-preview

Here is the review of the proposed **UI Workflow (Figma Sync + Xcode Validation)** plan:

### 1. P0/P1/P2 Risks
*   **P1 Risk (Process Overhead):** Mandating "Figma always-sync" and "strict iOS validation" for *all* UI changes risks grinding development to a halt for trivial updates (e.g., changing a hex code or tweaking padding by 2px). 
    *   *Mitigation:* Define a threshold for these requirements. Distinguish between "net-new components/screens" (requires full sync and validation) and "minor tweaks" (requires basic visual check).
*   **P2 Risk (Fragmentation):** Spreading the workflow rules across `PRODUCT_PLAN.md`, `agent-orchestration.md`, `figma-mcp-and-api.md`, and `rules.md` risks creating conflicting sources of truth over time.

### 2. Missing Steps & Hidden Dependencies
*   **Missing Creation Step:** The `Delegation` section references `docs/plans/2026-03-03-figma-xcode-ui-workflow.md`, but there is no explicit step to save this plan to that directory.
*   **Missing Automation Link:** If `docs/templates/agent-spec.md` is updated, are there any scaffolding scripts (e.g., `npm run create-agent-task`) that also need their inline templates updated to match?
*   **File Existence:** `.agents/memory/rules.md` is not explicitly listed in the current visible directory tree (only the `memory/` folder is). Ensure the step includes creating it if it doesn't exist.

### 3. Frontend State Coverage
*   *N/A - Process change only.* (However, the update to `agent-spec.md` correctly includes "states" in its validation checklist, which will enforce this for future UI work).

### 4. Accessibility Risks
*   *Positive Impact:* Forcing VoiceOver and Dynamic Type into the baseline checklist is an excellent addition. Ensure the docs explicitly mention testing Dynamic Type at the *maximum* accessibility size to catch overflow issues early.

### 5. Concrete Tests to Add/Run
*   **Add:** Run `npm run lint` (or a specific markdown linter like `markdownlint` given `.markdownlint.json` exists in the root) to ensure the newly added doc sections don't break formatting pipelines.
*   **Add:** Search the codebase for `agent-plan.md` to ensure *all* broken links to the old template are caught, not just the one in `docs/README.md`.

### 6. Simpler Alternatives
*   **Consolidate Documentation:** Instead of updating 4+ separate markdown files, create a single, authoritative `docs/ux/ui-development-workflow.md`. 
    *   Update `PRODUCT_PLAN.md`, `agent-orchestration.md`, and `.agents/memory/rules.md` to simply *point* to this new file. This makes future updates to the workflow a single-file change and reduces cognitive load.
